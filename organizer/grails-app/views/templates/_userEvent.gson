import organizer.Event

model {
    Event event
}

json {
    eventId event.id
    eventName event.name
    eventDescription event.description
    startingMonth event.startingMonth
    startingDay event.startingDay
    startingYear event.startingYear
    dueMonth event.dueMonth
    dueDay event.dueDay
    dueYear event.dueYear
    dueHour event.dueHour
    dueMinute event.dueMinute
    eventColor event.color
    ownerId event.user.id
    isPublic event.isPublic
}
